<?php

/**
 * Write here the code you need to run AFTER deploy the project.
 * Everything is ready to start working.
 *
 * Use this file to active services, set down maintenance...
 */

// if (file_get_contents('http://localhost/api_test') === '1') exec('rm -Rf ' . $backupDirectory);
